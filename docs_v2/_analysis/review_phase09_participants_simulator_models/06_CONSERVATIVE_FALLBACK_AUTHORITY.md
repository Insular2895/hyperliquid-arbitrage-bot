# Conservative Fallback Authority

A fallback may abstain, narrow scope, widen uncertainty and propose lower size within its own validated support. It cannot inherit primary support, increase `Q_validated`, activate Maker, override OOD/Risk, add unseen markets/regimes/sizes or self-promote. Missing/stale/OOD fallback means reject/disable the dependent capability.
