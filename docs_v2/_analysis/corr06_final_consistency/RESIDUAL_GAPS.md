# CORR-06 Residual Gaps

`INTERNAL BLOCKING CONSISTENCY GAPS: 0`

Non-blocking or consumer-blocking items intentionally remain:

- fast-cancel measurable prioritization effect: `DOC-SCOPE CONFLICT / EXTERNAL_REVALIDATION REQUIRED`;
- every mutable priority/API/node fact: revalidate at implementation/experiment time;
- optimal gossip slot/write rate and any PriorityOptimizer: uncalibrated/unapproved;
- provider/region/AZ/profile ranking: requires real simultaneous campaign;
- InfraProfile freshness thresholds/campaign length/support: calibrated, no invented numbers;
- actual IOC/partial/completion/Recovery/priority benefit: requires Shadow then authorized Micro-live;
- all existing `OPEN`, `CALIBRATED`, `LEARNED`, Research/Future items and HDC approval;
- human documentation approval, switchover and implementation authorization.

These do not prevent final human documentary review. They do prevent the affected implementation, promotion or capital claim.
