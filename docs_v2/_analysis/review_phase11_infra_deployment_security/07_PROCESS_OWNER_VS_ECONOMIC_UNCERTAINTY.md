# Process Owner vs Economic Uncertainty

Lock/process termination changes software ownership only. Orders may still exist/fill; UNKNOWN, reservations and actual exposure persist. A new process reconstructs journal/checkpoint, queries exchange, deduplicates fills, reconciles and only then may release claims/approach READY. No deployment event authorizes resend or economic rollback.
