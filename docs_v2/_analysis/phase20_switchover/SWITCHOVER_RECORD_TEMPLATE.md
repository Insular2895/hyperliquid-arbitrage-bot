# Switchover Record Template

`STATUS: FUTURE OUTPUT — COMMIT B ABSENT`

Required: record/version; `SwitchoverAuthorization(F)` ID; exact approved F SHA/tree/`docs_v2` tree/manifest from its DecisionRecord; source and overlay lineage through F; pre-state branch/HEAD/status and legacy/F trees; isolated worktree identity at exact F; transformation/retention policies; LinkRewriteManifest hash; normalized F→B artifact manifests; semantic-diff/link/count/tree/certificate results; exact B SHA/tree/ref after creation; deviations; author/time; push status; human acceptance record; rejection/rollback disposition. It grants no Phase-1 or capital authority.
