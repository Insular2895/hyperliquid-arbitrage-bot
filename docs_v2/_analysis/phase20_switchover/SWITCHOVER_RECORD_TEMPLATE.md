# Switchover Record Template

`STATUS: FUTURE OUTPUT — COMMIT B ABSENT`

Required: record/version; `SwitchoverAuthorization(C, semantic_candidate=A)` ID; exact A SHA/tree; exact C SHA/tree/`docs_v2` tree/manifest; A→C lineage; pre-state branch/HEAD/status and legacy/C trees; isolated worktree identity at C; transformation/retention policies; LinkRewriteManifest hash; normalized C→B artifact manifests; semantic-diff/link/count/tree/certificate results; exact B SHA/tree/ref after creation; deviations; author/time; push status; human acceptance record; rejection/rollback disposition. It grants no Phase-1 or capital authority.
