# Approved Documentation Manifest Template

`STATUS: PENDING — NOT APPROVED`

Fields: Candidate A SHA/tree; generated artifact list with source path/hash/class and normalized B destination; required/optional status; `_analysis`/`_review` retention; allowed-path and link transformation classes; traceability certificate identity; manifest hash; reviewer/decision/time; expiry/supersession. Counts are validation summaries, never authority over the manifest.
