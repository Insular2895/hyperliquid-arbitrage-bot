# Approved Documentation Manifest Template

`STATUS: PENDING — NOT APPROVED`

Fields: Semantic Candidate A SHA/tree; exact Review Envelope C SHA/tree/`docs_v2` tree; A→C attestation relationship; C-generated artifact list with source path/hash/class and normalized B destination; required/optional status; `_analysis`/`_review` retention; allowed-path and link transformation classes; Phase-18 A certificate plus A→C lineage identity; manifest hash; reviewer/`DocumentationApproval(A,C)` decision/time; expiry/supersession. Future switchover input is exact approved C, never moving HEAD or the older A tree. Counts are validation summaries, never authority over the manifest.
