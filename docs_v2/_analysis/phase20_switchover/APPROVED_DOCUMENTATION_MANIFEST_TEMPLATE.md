# Approved Documentation Manifest Template

`STATUS: PENDING — NOT APPROVED`

Fields: exact Final Review Candidate F SHA/tree/`docs_v2` tree from the human DecisionRecord; F artifact list with source path/hash/class and normalized B destination; required/optional status; `_analysis`/`_review` retention; allowed-path and link transformation classes; Phase-18 certificate plus source/overlay lineage through F; manifest hash; reviewer/`DocumentationApproval(F)` decision/time; expiry/supersession. Future switchover input is exact approved F, never moving HEAD, the latest branch tip or historical A/C. Counts are validation summaries, never authority over the manifest.
