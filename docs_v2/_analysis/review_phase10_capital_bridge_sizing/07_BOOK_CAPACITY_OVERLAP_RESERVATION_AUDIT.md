# Book Capacity Overlap Reservation Audit

Reservation A over levels 1–3 and B over 2–5 share levels 2–3; both cannot claim them. Any overlapping executable units are deducted once at sufficient granularity. Per-level/interval/segment representation is an implementation choice. A BookVersion change invalidates stale capacity authority and triggers final revalidation.
