# Public Feed vs Node Comparison Contract

`STATUS: SPECIFIED — NODE OBSERVE-ONLY FIRST`

## Comparable evidence

Hold bot build, market universe, normalization semantics, formulas/models, host where possible, interval and instrumentation constant. Compare only proven-equivalent events. Report node-first, public-first, ties/inconclusive, lead/lag distribution and P50/P95/P99/P99.9/max where supported, plus unmatched counts.

## Two required result layers

1. Information quality: continuity, gaps, reorder, duplicates, lag/catch-up, reconnect, state reconstruction, resource/interference and usable state age.
2. Downstream effect: opportunity/funnel movement, state age at send, would-attempts, edge survival, actual completion/Recovery/`UNKNOWN` evidence when valid, and QF-086–QF-091-compatible economics.

Earlier raw arrival alone cannot promote. A faster feed with worse state validity, reliability, security or net value may lose. Same-host paired monotonic timing is preferred; cross-host leads within combined clock uncertainty are `INCONCLUSIVE_CLOCK`.
