# Repo-Wide Execution Consistency

Searches across all documented states, IOC/cancel/UNKNOWN, plans, actual/expected quantities, RiskDecision fields/actions, Recovery context, transport and reservations found no surviving affirmative permission to mutate a plan, propagate predicted fills, release on cancel request, blind-retry, treat `allowed` alone as permission or use Recovery-only for normal risk. The unresolved Boolean/action mapping is explicit and fail-closed.
