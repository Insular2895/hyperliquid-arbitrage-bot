# Build / Validate / Scale Evidence Stages

DOCUMENTATION STATUS:
AWAITING HUMAN REVIEW

## Scientific order

```text
SPECIFY → OBSERVE/RECORD → RECONSTRUCT → MAP → IDENTIFY
→ REPLAY → SIMULATE → SHADOW LIVE → PREDECLARE COMPARISONS
→ LEARN COMPETITION → MICRO-LIVE → VALIDATE TT → VALIDATE TTT
→ MAKER INTELLIGENCE → VALIDATE MT/MTT → CAPITAL INTELLIGENCE
→ PORTFOLIO → BRIDGE → HORIZONTAL → VERTICAL → INFRA SCALE
```

## Evidence classes by stage family

| Family | Stages | What becomes knowable | What remains unknowable |
|---|---|---|---|
| Specification | 0 | Contract completeness and planned evidence | Runtime correctness/value |
| Source/state | 1–2 | What arrived and whether state reconstructs | Economic opportunity/execution |
| Structure/economics | 3–4 | Legal routes and exact current-book candidate economics | Real capture/fill/causal response |
| Historical experiment | 5–6 | Deterministic behavior and explicit-fidelity distributions | Live-system and intervention truth |
| Live no-effect | 7–9 | Real-time stability, forecasts, survival/response observation | Own fill/impact/recovery truth |
| Controlled intervention | 10–14 | Actual exchange/mechanical/mode calibration | Unprobed market/mode/q regimes |
| Capital productivity | 15–17 | Utility, shared allocation and relocation evidence | Unsupported future capacity |
| Scale | 18–20 | Wider/deeper/infra-specific value inside support | Unlimited/generalized scale |

## Exit law

A stage exits only when its evidence is sufficient, reproducible, supported and current for the declared capability. An elapsed period or deposited amount is not an exit criterion. Each exit may be positive (promote), neutral (hold/retain baseline) or negative (reject/demote/shrink). See [Evidence Stage Exit Criteria](../../_analysis/pass12_build_validate_scale/EVIDENCE_STAGE_EXIT_CRITERIA.md).

## Persistent instruments

- Recorder continues to preserve research, Shadow, live, execution and incident evidence.
- Replay continues for regression, incidents, model/calibration/capacity research and release validation.
- Shadow continues for new markets, models, modes, releases, sizes and hosts.
- Micro-live is reusable for a new execution mode, support region, host or changed exchange behavior.

## Why each major transition exists

- Record before prediction so labels can be audited against source truth.
- Reconstruct before alpha so false books do not create false opportunities.
- Map before selection so structure and opportunity are not conflated.
- Replay before Live so deterministic/failure behavior is reproducible.
- Shadow before capital so real-time defects are found without strategy effects.
- Micro-live before scaling because real fills/fees/impact/Recovery cannot be inferred completely from Shadow.
- TT before maker because it removes queue/resting/adverse-selection uncertainty.
- Simple baseline before complexity so added value is measurable.
- Horizontal before excessive vertical size because one route has finite nonlinear capacity.
- Benchmark infra before paying more; economic promotion waits for captured/lost-PnL evidence.
- Validate Bridge separately because it moves capital for future utility rather than arbitrage alpha.

CORR-04 infrastructure order is `public baseline → measure → observe-only feed/node challenger → strict correctness/alignment → downstream capture evidence → robust net value → explicit promotion → deeper host/network complexity`. Shadow is the primary challenger stage. Docker remains baseline; native/tuning/kernel bypass are independent scoped candidates and cannot inherit maturity.
