# Dataset and Run Index

STATUS: ZERO REGISTRY ROWS

| `DatasetId` | `RunId` | Manifest/hash | Mode/fidelity | Time scope | Evidence/report | Validity |
|---|---|---|---|---|---|---|

Canonical typed identities and `RunManifest` remain authoritative. Human-readable `DATASET-*` and `RUN-*` labels, if later adopted, must be reversible serializations rather than second identities.
